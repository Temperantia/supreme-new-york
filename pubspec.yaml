name: supreme
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http_proxy: 1.1.0
  flutter_inappwebview: 5.1.0+4
flutter:
  uses-material-design: true
